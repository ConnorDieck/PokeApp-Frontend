import React from "react";

/** Generic 404 component. */

const NotFound = () => {
	return (
		<div>
			<p>Sorry, we weren't able to locate that page for you.</p>
		</div>
	);
};

export default NotFound;
